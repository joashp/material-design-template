node ('agent')	
{
    stage ('Build NodeJS')
    {    
        env.NODEJS_HOME = tool 'nodejs16'
		println(env.NODEJS_HOME)
		ln -s /usr/bin/nodejs /usr/bin/node
    }
    stage ('Checkout Repository')
    {
        checkout scm
    }
	stage ('Compressing JS and CSS')
	{
	    parallel_steps = [ "js" : {sh '${NODEJS_HOME}/bin/uglifyjs ./www/js/* -c -o ./www/min/compressed-js.js'},
                            "css" : {sh '${NODEJS_HOME}/bin/cleancss ./www/css/* -o ./www/min/compressed-css.css'}]
        parallel parallel_steps
    }
    stage ('Archiving files into TAR')
    {
        sh 'tar --exclude-vcs --exclude=www/js --exclude=www/css -cvf build.tar .'    
    }
    stage ('Archiving artifacts to Jenkins')
    {
        archiveArtifacts artifacts: '*.tar', followSymlinks: false    
    }
}
